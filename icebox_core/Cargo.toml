[package]
name = "icebox_core"
version = "0.1.0"
edition = "2018"

[features]
std = []

[dependencies]
kvm-common = {path = "../kvm-common", features = ["serde"]}

bytemuck = {version = "1.7", features = ["derive"]}
